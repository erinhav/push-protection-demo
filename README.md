# Moved

This demo now lives in **`github/push-protection-demo`** (private to the `github` org).

This copy is archived and read-only. Do not use it — it will not receive updates,
and a stale demo repo is exactly how a live demo fails in front of an audience.

Ask in the Secret Protection channel if you need access to the canonical repo.
